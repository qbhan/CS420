int avg (int count, int *value)
{
    int i, total;
    total = 0;
    for (i = 0; i < count; i++)
    {
        total = total + value[i];
    }
    return (total / count);
}
int lm ()
{
    return 1;
}

int test (int param)
{
    int i;
    if (param > 1)
    {
   for (i = 0; i< param; i++)
   {
       printf ("a");
        }
    }
    else
    {
        printf ("b");
    }
    return 1;
}
int main (void)
{
    int student_number, count, i, sum, a, b;
    int mark[4];
    float average;
    count = 4;
    sum = 0;
    for (i = 0; i < count; i++)

        mark[i] = i * 30;
        sum = sum + mark[i];
        average = avg(i + 1, mark);
        if(average > 40)
        {
            printf ("%f\n", average);
        }
    }
    a = test(2);
    return a;
}